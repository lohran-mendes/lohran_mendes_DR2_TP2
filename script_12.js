// “script_12.js” → Crie um programa que contenha os seguintes itens:
// Crie um comentário explicando a diferença entre let e const. Mostre exemplos de uso.
// Crie um comentário explicando porque utilizar var ou variáveis não declaradas é uma má prática.

// A diferença entre let e const é que embora ambas sejam maneiras de declarar variáveis o const é usado para variáveis cujo o seu valor não poderá ser mudado ou re-atribuido enquanto o let possibilita essa mudança e é usado quando se tem a intenção de alterar o valor desta.

// o uso do var não é recomendável por diversos motivos, um deles é porque ela não funciona com o escopo de bloco, podendo vazar a variável que foi declarada com var, outro motivo é porque ela possibilita o caso de declarar uma variável já declarada anteriormente sem dar erro, enquanto o let e o const não permitem(que é o comportamento correto). 